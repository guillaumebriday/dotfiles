name: Lint

on: [push]

jobs:
  yamllint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: reviewdog/action-shellcheck@master
        with:
          path: "."
          pattern: "*.sh"
